{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bienvenue sur notre page d'accueil</h1>
    <div class="row">
        <div class="tournament_list_wrapper">
            {% for tournament in tournaments %}
                    <div class="tournament_tile">
                        {{ tournament.name }}
                    </div>

            {% endfor %}
            {% if true %}

                <a href="{{ path('create_tournament')}}">
                    <div class="tournament_tile">
                        Ajouter un tournoi
                    </div>
                </a>

            {% endif %}
        </div>
    </div>
{% endblock %}